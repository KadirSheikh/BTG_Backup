import { NoRedCardPipe } from './no-red-card.pipe';

describe('NoRedCardPipe', () => {
  it('create an instance', () => {
    const pipe = new NoRedCardPipe();
    expect(pipe).toBeTruthy();
  });
});
